# ==============================================================================
# PLANTILLA DE SECRETOS (SECRETS.TOML) - ENTORNO B2B (PRODUCCIÓN / DEMO)
# ==============================================================================
# INSTRUCCIONES PARA DESPLIEGUE (Solo para administradores):
# 1. Copiar este archivo y renombrarlo a `secrets.toml` en el entorno local.
# 2. Reemplazar los valores simulados por las credenciales reales de Google Cloud.
# 3. En Streamlit Community Cloud, inyectar este bloque en "Advanced Settings > Secrets".
# NOTA DE SEGURIDAD: El archivo `secrets.toml` con claves reales NUNCA debe
# subirse al repositorio público. Está protegido mediante .gitignore.

# ------------------------------------------------------------------------------
# 1. ENRUTAMIENTO DEL ORQUESTADOR (BACKEND - CLOUD RUN)
# ------------------------------------------------------------------------------
CLOUD_RUN_URL = "https://blue-switch-api-demo.europe-southwest1.run.app/optimizar"
CLOUD_RUN_HEALTH_URL = "https://blue-switch-api-demo.europe-southwest1.run.app/health"

# ------------------------------------------------------------------------------
# 2. CREDENCIALES DE IDENTIDAD GCP (DATA WAREHOUSE - BIGQUERY)
# ------------------------------------------------------------------------------
CLOUD_RUN_EJECUTIVO_URL = "https://blue-switch-api-demo.europe-southwest1.run.app/ejecutivo"